<%= form_for(piteu) do |f| %>
  <% if piteu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(piteu.errors.count, "error") %> prohibited this piteu from being saved:</h2>

      <ul>
      <% piteu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
